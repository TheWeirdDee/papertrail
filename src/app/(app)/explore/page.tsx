import ExploreContent from './ExploreContent';

export const metadata = {
  title: 'Explore Documents — PaperTrail',
  description: 'Browse recently registered documents on the PaperTrail blockchain registry.',
};

export default function ExplorePage() {
  return <ExploreContent />;
}
