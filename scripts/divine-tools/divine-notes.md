# Divine Bot Notes

- Keep `AUTO_SET_USERNAME=0` for existing wallets.
- Watch for `u103` errors when attempting username operations.
- Use `divine-state-report.ts` to inspect saved progress.
- Use `google sheets` or CSV exports to track wallet spending outside the bot.
